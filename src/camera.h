#ifndef CAMERA_H
#define CAMERA_H

#include <iostream>
#include <glm/glm.hpp>

class Camera {
public:

private:

//Camera position and position of target
// glm::vec3 camPos, targetPos;

//Spherical orientation from target
double phi, theta, r;

};
#endif